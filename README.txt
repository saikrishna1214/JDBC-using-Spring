a connection is established to our database and we perform CRUD operations on a table in our database.
after doing required operations connection is closed.
CRUD operations are performed by selecting a number which are assigned to them.
NOTE: provide database url, username , password in cofig.properties file. if file is not there then pass those values as JVM arguments










